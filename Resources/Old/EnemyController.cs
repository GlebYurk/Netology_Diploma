using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace WorldMode
{
    namespace Enemy
    {
        /*
        public class EnemyController
        {

            List<PatchHolder> _path;
            float _timer;

            public EnemyController(GameObject _actionObject,List<Tile> Spawner)
            {
                _path = new List<PatchHolder>();
                OnLevel1(Spawner);

            }
            public void addPatch(Tile tile, string patch)
            {
               // Patch patchBuff = new Patch();
               // patchBuff.path = patch;
              //  patchBuff.spawner = tile;
            }

            //Подг(Необходимо реализовать сериалтзацию, для уровней)
            private void OnLevel1(List<Tile> Spawner)
            {
                _path.Clear();
                addPatch(Spawner[0], "");
                addPatch(Spawner[0], "");

            }

        }
        */
    }
}